-- this is a vim plugin so we cannot configure it
-- This plugin automatically adjusts 'shiftwidth' and 'expandtab' heuristically based on the current file
-- no versin available
return {
	"tpope/vim-sleuth",
}
